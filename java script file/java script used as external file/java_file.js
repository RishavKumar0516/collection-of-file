
	   function welcome_msg(msg){
			alert(msg+"  "+"Everyone");/*adding space between the two words by using space bar inside semi-colon*/
			var name=prompt("Enter your Name");
			return name;
		}
			function message(nm){
		alert(nm);
		document.write(nm+"  "+"chaurasiya");/*adding space between the two words by using space bar inside semi-colon*/
	}
	function position(idno){
		alert("correcr id"+"  "+idno);
	}